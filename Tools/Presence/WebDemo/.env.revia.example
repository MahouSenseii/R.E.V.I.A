# Copy to .env.revia locally. Do not commit secrets. Parsed as data by WebDemo.ps1.
REVIA_WEB_ENABLED=1
REVIA_WEB_PORT=17864
# Create your own independent random secret; blank deliberately fails validation.
REVIA_WEB_LOCAL_TOKEN=
